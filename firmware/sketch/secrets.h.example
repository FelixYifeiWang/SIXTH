#pragma once
// Template for firmware/sketch/secrets.h — copy this file, rename to
// secrets.h, and fill in your 2.4 GHz WiFi credentials. secrets.h is
// gitignored so real credentials never leave your machine.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
