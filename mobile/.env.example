# Copy to mobile/.env and fill in the IP that setup_board.py wrote to
# .board.conf (or read it off the Serial monitor after the sketch boots —
# it prints "Mobile LIVE page: http://<ip>/report").
#
# Leave unset to force the LIVE page into its OFFLINE state.
EXPO_PUBLIC_BOARD_HOST=192.168.1.42
