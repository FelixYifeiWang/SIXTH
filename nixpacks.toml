[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["cd server && pip install -r requirements.txt"]

[start]
cmd = "cd server && uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}"
